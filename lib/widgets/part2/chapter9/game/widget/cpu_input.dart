import 'package:flutter/material.dart';

class CpuInput extends StatelessWidget {
  final bool isDone;

  const CpuInput({super.key, required this.isDone});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
